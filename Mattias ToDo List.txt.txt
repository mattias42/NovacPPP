CSpectrometerModel kan bara lista i förväg kända modeller... Möjlighet att lägga till fler modeller i config filer??

Lägg till en ersättning för CDialog *pView, det är denna konstruktion som
	gör det möjligt att logga data...


sök eftr strlen(FLAG(VARIABELNAMN)) och ersätt med FLAG(VARIABELNAMN).size()

	search for:
		strlen\(FLAG\((.*?)\)\)
	replace with 
		FLAG($1).size()
		
